# Core dependencies
pyyaml>=6.0
cryptography>=41.0.0
# Note: We implement a lightweight CEL-compatible evaluator inline
# This avoids external dependencies while supporting Cerbos policy syntax

# Optional dependencies (installed via extras_require)
# LLM providers (install with: pip install glasstape[llm])
# openai>=1.0.0
# anthropic>=0.7.0